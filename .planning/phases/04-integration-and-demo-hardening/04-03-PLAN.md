---
phase: 04-integration-and-demo-hardening
plan: 03
type: execute
wave: 3
depends_on: [04-01, 04-02]
files_modified:
  - frontend/src/api/finrag.ts
  - frontend/src/App.tsx
  - .planning/frontend-integration-readiness.md
  - .planning/phases/04-integration-and-demo-hardening/04-VALIDATION.md
autonomous: true
requirements: [INTG-02, INTG-03, INTG-04, INTG-05]
user_setup: []
must_haves:
  truths:
    - Backend and frontend validation commands pass or any environment blocker is documented.
    - Demo smoke checklist covers all three required questions.
    - Error/fallback behavior is documented and testable.
    - Phase 4 completion artifacts clearly hand off any remaining manual browser verification.
  artifacts:
    - .planning/phases/04-integration-and-demo-hardening/04-VALIDATION.md
    - .planning/frontend-integration-readiness.md
  key_links:
    - Validation includes `cd backend && python3 -m pytest` and frontend TypeScript/build commands.
    - Manual UAT items map to the three demo questions.
---

<objective>
Harden and validate the integrated demo path: run backend and frontend checks, document the final event-to-state mapping, capture manual smoke steps, and prepare Phase 4 for completion.

Purpose: ensure the integrated frontend/backend demo is reliable enough for interview demonstration and leaves clear instructions for any human browser checks.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
@/Users/jamiezhao/.codex/skills/karpathy-guidelines/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/frontend-integration-readiness.md
@.planning/phases/04-integration-and-demo-hardening/04-CONTEXT.md
@.planning/phases/04-integration-and-demo-hardening/04-RESEARCH.md
@.planning/phases/04-integration-and-demo-hardening/04-VALIDATION.md
@.planning/phases/03-agent-workflow-and-sse-query-api/03-VERIFICATION.md
@backend/tests/test_query_api.py
@frontend/package.json
@frontend/src/App.tsx
@frontend/src/api/finrag.ts
</context>

<threat_model>
## Threat Model

### Assets
- Interview demo reliability.
- Accurate documentation of what was actually validated.
- Offline/test fallback behavior.

### Threats
- **High:** Declaring integration complete without running frontend validation.
  - Mitigation: run `npm run lint` and `npm run build`; document blockers if dependencies/environment fail.
- **High:** Manual browser-only behavior remains untracked.
  - Mitigation: create/update validation checklist with explicit UAT steps and expected outcomes.
- **Medium:** Frontend build passes but backend contract drifts later.
  - Mitigation: rely on adapter types/mappers and backend query tests; do not skip backend pytest.
- **Medium:** Demo fallback behavior is unclear to user.
  - Mitigation: document backend-unavailable and SSE-error behavior.

### Blocking Requirement
Do not mark Phase 4 complete if backend or frontend validation fails for Phase 4-related reasons.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add or refine focused adapter/parser checks</name>
  <files>frontend/src/api/finrag.ts, frontend/package.json</files>
  <read_first>frontend/package.json, frontend/src/api/finrag.ts</read_first>
  <action>If the existing frontend tooling supports it without heavy setup, add lightweight tests or scriptable checks for mapper/parser pure functions. If not, keep parser/mappers simple and rely on TypeScript validation. Do not add a large test framework just for this phase.</action>
  <verify>cd frontend && npm run lint</verify>
  <acceptance_criteria>
    - Mapper/parser logic is either covered by lightweight checks or remains simple and typechecked.
    - No heavyweight testing infrastructure is introduced unnecessarily.
  </acceptance_criteria>
  <done>Adapter correctness has an appropriate validation path.</done>
</task>

<task type="auto">
  <name>Task 2: Update integration readiness and validation docs</name>
  <files>.planning/frontend-integration-readiness.md, .planning/phases/04-integration-and-demo-hardening/04-VALIDATION.md</files>
  <read_first>.planning/frontend-integration-readiness.md, .planning/phases/04-integration-and-demo-hardening/04-VALIDATION.md, frontend/src/App.tsx</read_first>
  <action>Update docs to reflect the final Phase 4 wiring: document endpoint mapping, SSE event-to-state mapping, fallback behavior, validation commands, and manual smoke checklist for the three demo questions. Keep docs factual to implemented behavior.</action>
  <verify>grep -q "Phase 4" .planning/frontend-integration-readiness.md && grep -q "npm run build" .planning/phases/04-integration-and-demo-hardening/04-VALIDATION.md</verify>
  <acceptance_criteria>
    - Docs describe actual implemented files/behavior.
    - Manual UAT steps are clear and tied to the three demo questions.
    - Any validation limitations are explicit.
  </acceptance_criteria>
  <done>Integration handoff documentation is current.</done>
</task>

<task type="auto">
  <name>Task 3: Run backend and frontend validation</name>
  <files>backend/tests/*, frontend/src/*, frontend/package.json</files>
  <read_first>.planning/phases/04-integration-and-demo-hardening/04-VALIDATION.md</read_first>
  <action>Run `cd backend && python3 -m pytest`, `cd frontend && npm run lint`, and `cd frontend && npm run build`. Fix only Phase 4-related failures. If frontend dependencies are missing, install only if consistent with project setup and do not alter lockfiles unexpectedly unless necessary.</action>
  <verify>cd backend && python3 -m pytest && cd ../frontend && npm run lint && npm run build</verify>
  <acceptance_criteria>
    - Backend tests pass.
    - Frontend TypeScript validation passes.
    - Frontend production build passes, or an environment blocker is documented.
  </acceptance_criteria>
  <done>Integrated demo path has automated validation evidence.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd backend && python3 -m pytest`
- [ ] `cd frontend && npm run lint`
- [ ] `cd frontend && npm run build`
- [ ] Confirm docs list the three manual demo questions.
- [ ] Confirm no real API keys or production deployment assumptions were added.
</verification>

<success_criteria>
- Phase 4 has validated frontend/backend integration code.
- Demo smoke path and fallback behavior are documented.
- UI remains externally owned and visually preserved.
- Remaining manual browser verification, if any, is tracked clearly.
</success_criteria>

<output>
After completion, create `.planning/phases/04-integration-and-demo-hardening/04-03-SUMMARY.md` and update Phase 4 completion state through the GSD workflow.
</output>
