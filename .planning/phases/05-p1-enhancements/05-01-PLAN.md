---
phase: 05-p1-enhancements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/api/preview_rewrite.py
  - backend/app/main.py
  - backend/app/core/agent/query_analysis.py
  - backend/tests/test_preview_rewrite.py
autonomous: true
requirements: [API-10]
user_setup: []
must_haves:
  truths:
    - `POST /api/preview-rewrite` is lightweight, deterministic, and reuses existing query analysis.
    - Response includes expanded terms, sub-queries, and detected entities.
    - Main `POST /api/query` SSE workflow remains unchanged.
    - Preview tests do not require live API keys.
  artifacts:
    - backend/app/api/preview_rewrite.py
    - backend/app/core/agent/query_analysis.py
    - backend/tests/test_preview_rewrite.py
  key_links:
    - Existing query rewrite logic already lives in `backend/app/core/agent/query_analysis.py`.
    - `backend/app/main.py` must register the new preview router.
---

<objective>
Add the optional backend query rewrite preview endpoint for Phase 5.

Purpose: expose deterministic rewrite preview data that matches the main analysis logic and can be used by the frontend typing preview.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
@/Users/jamiezhao/.codex/skills/karpathy-guidelines/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-p1-enhancements/05-CONTEXT.md
@.planning/phases/05-p1-enhancements/05-RESEARCH.md
@.planning/phases/05-p1-enhancements/05-VALIDATION.md
@backend/app/core/agent/query_analysis.py
@backend/app/models/events.py
@backend/app/models/schemas.py
@backend/app/main.py
</context>

<threat_model>
## Threat Model

### Assets
- Query rewrite consistency.
- Main SSE workflow stability.
- Demo latency and reliability.

### Threats
- **High:** Preview endpoint drifts from actual query rewrite behavior.
  - Mitigation: reuse `analyze_query()` directly instead of duplicating logic.
- **Medium:** Preview endpoint accidentally changes the main query flow.
  - Mitigation: add a new router and keep `/api/query` untouched.
- **Medium:** Preview endpoint requires live provider access.
  - Mitigation: keep it deterministic and offline-testable.

### Blocking Requirement
No changes to the main SSE query workflow are allowed in this plan.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add preview rewrite endpoint</name>
  <files>backend/app/api/preview_rewrite.py, backend/app/main.py</files>
  <read_first>backend/app/core/agent/query_analysis.py, backend/app/models/schemas.py, .planning/phases/05-p1-enhancements/05-CONTEXT.md</read_first>
  <action>Create `POST /api/preview-rewrite` using the existing `QueryRequest` or a small dedicated request schema. Return the deterministic rewrite payload with original query, expanded terms, sub-queries, detected entities, and intent if helpful. Register the router in `backend/app/main.py`.</action>
  <verify>cd backend && python3 -m pytest tests/test_preview_rewrite.py</verify>
  <acceptance_criteria>
    - Preview response includes expanded terms and detected entities.
    - Preview response stays aligned with `analyze_query()`.
    - Main `/api/query` route is unaffected.
  </acceptance_criteria>
  <done>Backend preview rewrite endpoint is available for frontend typing preview.</done>
</task>

<task type="auto">
  <name>Task 2: Add deterministic entity extraction</name>
  <files>backend/app/core/agent/query_analysis.py, backend/tests/test_preview_rewrite.py</files>
  <read_first>backend/app/core/agent/query_analysis.py, .planning/phases/05-p1-enhancements/05-RESEARCH.md</read_first>
  <action>Add a minimal helper for detecting known company/entities from the query so the preview response can surface them explicitly. Keep the helper deterministic and aligned with the existing alias map. Do not add a broad NER system or external dependency.</action>
  <verify>cd backend && python3 -m pytest tests/test_preview_rewrite.py</verify>
  <acceptance_criteria>
    - Known companies/aliases are returned as detected entities.
    - Helper remains deterministic and cheap.
    - Tests cover at least the two demo companies and a no-entity case.
  </acceptance_criteria>
  <done>Preview response can expose detected entities for a richer typing preview.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd backend && python3 -m pytest tests/test_preview_rewrite.py`
- [ ] Confirm `POST /api/query` route was not modified beyond router registration if needed.
- [ ] Confirm preview logic reuses existing query analysis.
</verification>

<success_criteria>
- The optional preview endpoint is deterministic, lightweight, and safe.
- Preview data matches the real query rewrite behavior.
- Demo path remains unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/05-p1-enhancements/05-01-SUMMARY.md`.
</output>
