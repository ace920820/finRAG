---
phase: 01-backend-foundation-and-demo-data
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - backend/tests/conftest.py
  - backend/tests/test_health.py
  - backend/tests/test_documents_api.py
  - backend/tests/test_schemas.py
  - backend/tests/test_seed_data.py
  - backend/README.md
autonomous: true
requirements: [BACK-04, API-01, DATA-01, DATA-05]
user_setup: []
must_haves:
  truths:
    - Test suite passes without external API calls or credentials.
    - Tests cover health route, document API shape, schema serialization, and fixture regeneration.
    - Backend README documents how to run the app, seed fixture data, and run tests.
  artifacts:
    - backend/tests/test_documents_api.py
    - backend/tests/test_schemas.py
    - backend/tests/test_seed_data.py
    - backend/README.md
  key_links:
    - backend/README.md commands match the actual scripts and tests.
    - tests import backend/app code rather than duplicating response shapes.
---

<objective>
Add Phase 1 automated validation and backend developer documentation.

Purpose: prove the backend foundation meets Phase 1 success criteria and give future frontend/import work stable commands and contract references.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-backend-foundation-and-demo-data/01-CONTEXT.md
@.planning/phases/01-backend-foundation-and-demo-data/01-RESEARCH.md
@.planning/phases/01-backend-foundation-and-demo-data/01-VALIDATION.md
@FinRAG_需求文档.md
</context>

<threat_model>
## Threat Model

### Assets
- Test fixtures and contract examples that future agents/frontend work will trust.
- Local developer commands documented in README.

### Threats
- **Medium:** Tests pass by duplicating expected payloads instead of exercising real app code.
  - Mitigation: endpoint tests must call `create_app()` with FastAPI TestClient and schema tests must instantiate real Pydantic models.
- **Medium:** README encourages real API key usage too early.
  - Mitigation: document mock/demo mode as default and note real providers are later phases.
- **Low:** Tests become order-dependent because scripts mutate data unexpectedly.
  - Mitigation: seed script should be deterministic and idempotent.

### Blocking Requirement
All Phase 1 validation must pass without network access or credentials.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add pytest configuration and app fixture</name>
  <files>backend/tests/conftest.py</files>
  <read_first>backend/app/main.py</read_first>
  <action>Create pytest fixtures for `app` and `client` using FastAPI TestClient. Ensure tests can import the backend package when run from `backend/`. Keep fixtures simple and deterministic.</action>
  <verify>cd backend && python -m pytest --collect-only</verify>
  <acceptance_criteria>
    - Pytest can collect tests from `backend/tests`.
    - TestClient fixture uses `backend.app.main:create_app` or equivalent actual app factory.
  </acceptance_criteria>
  <done>Test harness can exercise the real FastAPI app.</done>
</task>

<task type="auto">
  <name>Task 2: Add health and document API tests</name>
  <files>backend/tests/test_health.py, backend/tests/test_documents_api.py</files>
  <read_first>FinRAG_需求文档.md, backend/app/api/documents.py</read_first>
  <action>Add tests verifying `/health` returns OK metadata and `/api/documents` returns HTTP 200 with `total` and `documents`. Validate every document item includes `doc_id`, `title`, `doc_type`, `company`, `date`, and `chunk_count`. Assert at least the Moutai and CATL documents exist.</action>
  <verify>cd backend && python -m pytest tests/test_health.py tests/test_documents_api.py</verify>
  <acceptance_criteria>
    - Health endpoint test passes.
    - Document endpoint test validates exact frontend-facing fields.
    - Tests prove fixture data includes Guizhou Moutai and CATL.
  </acceptance_criteria>
  <done>API contract tests cover Phase 1 endpoints.</done>
</task>

<task type="auto">
  <name>Task 3: Add schema and seed-data tests</name>
  <files>backend/tests/test_schemas.py, backend/tests/test_seed_data.py</files>
  <read_first>backend/app/models/schemas.py, backend/app/models/events.py, backend/scripts/seed_data.py, backend/scripts/build_index.py</read_first>
  <action>Add tests that instantiate and serialize Document, Chunk, DocumentListResponse, QueryRequest, and representative SSE event payload models. Add tests or subprocess checks that `seed_data.py` and `build_index.py --fixture-only` run successfully and validate deterministic fixture outputs.</action>
  <verify>cd backend && python -m pytest tests/test_schemas.py tests/test_seed_data.py</verify>
  <acceptance_criteria>
    - Schema tests cover both REST and future SSE event payload models.
    - Seed/build script tests pass without external services.
    - Tests do not require environment variables for API keys.
  </acceptance_criteria>
  <done>Data and schema validation are automated.</done>
</task>

<task type="auto">
  <name>Task 4: Document backend run and validation commands</name>
  <files>backend/README.md</files>
  <read_first>.planning/phases/01-backend-foundation-and-demo-data/01-CONTEXT.md, .planning/phases/01-backend-foundation-and-demo-data/01-VALIDATION.md</read_first>
  <action>Create a concise backend README with setup, install, run, seed data, fixture-only build, and test commands. Document that frontend UI is external and this backend exposes `/api/documents` now while `/api/query` arrives in later phases. Include the expected local base URL `http://localhost:8000`.</action>
  <verify>test -f backend/README.md && grep -q "/api/documents" backend/README.md && grep -q "pytest" backend/README.md</verify>
  <acceptance_criteria>
    - README includes install and run commands.
    - README includes seed/build/test commands.
    - README names `/api/documents` and notes `/api/query` is later-phase work.
  </acceptance_criteria>
  <done>Backend foundation is documented for developers and integration work.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m compileall backend/app backend/scripts`
- [ ] `cd backend && python -m pytest`
- [ ] `cd backend && python scripts/seed_data.py`
- [ ] `cd backend && python scripts/build_index.py --fixture-only`
- [ ] Confirm no test requires external API keys or network.
</verification>

<success_criteria>
- Phase 1 has executable automated tests.
- Backend README documents local setup and validation commands.
- Tests cover app importability, endpoint contract, schemas, and deterministic demo data.
- All validation runs without external providers.
</success_criteria>

<output>
After completion, create `.planning/phases/01-backend-foundation-and-demo-data/01-03-SUMMARY.md`.
</output>
