---
phase: 03-agent-workflow-and-sse-query-api
plan: 03
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - backend/tests/test_query_api.py
  - backend/tests/test_agent_workflow.py
  - backend/tests/test_provider_config.py
  - backend/.env.example
  - .planning/frontend-integration-readiness.md
  - .planning/phases/03-agent-workflow-and-sse-query-api/03-VALIDATION.md
autonomous: true
requirements: [AGNT-01, AGNT-02, AGNT-03, AGNT-04, AGNT-05, AGNT-06, API-02, API-03, API-06, API-07, API-08, API-09]
user_setup: []
must_haves:
  truths:
    - Full backend test suite passes offline.
    - Phase 3 SSE contract examples are documented for Phase 4 frontend integration.
    - `.env.example` still exposes blank Bailian/Qwen Plus config without secrets.
    - No frontend UI redesign or adapter implementation is included in this phase.
  artifacts:
    - backend/tests/test_query_api.py
    - backend/.env.example
    - .planning/frontend-integration-readiness.md
    - .planning/phases/03-agent-workflow-and-sse-query-api/03-VALIDATION.md
  key_links:
    - Tests validate the exact backend contract that Phase 4 adapters will consume.
    - Frontend readiness doc records event-to-state mapping but does not change React UI.
---

<objective>
Harden Phase 3 with contract-focused tests, environment template verification, and frontend integration notes so the backend query SSE API is ready for Phase 4 wiring.

Purpose: close the phase with repeatable validation evidence and a clear handoff contract for frontend/backend联调.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
@/Users/jamiezhao/.codex/skills/karpathy-guidelines/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/frontend-integration-readiness.md
@.planning/phases/03-agent-workflow-and-sse-query-api/03-CONTEXT.md
@.planning/phases/03-agent-workflow-and-sse-query-api/03-RESEARCH.md
@.planning/phases/03-agent-workflow-and-sse-query-api/03-VALIDATION.md
@backend/.env.example
@backend/tests/test_query_api.py
@frontend/src/App.tsx
@frontend/src/components/ChatArea.tsx
@frontend/src/components/SidebarRight.tsx
</context>

<threat_model>
## Threat Model

### Assets
- Backend/frontend API contract.
- Deterministic demo behavior.
- Provider configuration placeholders.

### Threats
- **High:** Phase is marked complete while frontend-critical fields are missing.
  - Mitigation: assert payload keys for retrieval, rerank, chunks, done citations, and errors.
- **Medium:** Documentation promises fields not covered by tests.
  - Mitigation: update docs from implemented contract only.
- **Medium:** `.env.example` loses required Bailian blanks.
  - Mitigation: add/keep tests or manual verification for required variables.
- **Low:** Phase 3 accidentally starts Phase 4 frontend implementation.
  - Mitigation: documentation only; no React rewiring in this plan.

### Blocking Requirement
Do not introduce tests that require network access or real Alibaba Cloud credentials.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Strengthen endpoint contract tests</name>
  <files>backend/tests/test_query_api.py, backend/tests/test_agent_workflow.py</files>
  <read_first>.planning/frontend-integration-readiness.md, backend/app/models/events.py, backend/app/api/query.py</read_first>
  <action>Extend tests to parse the SSE stream and assert frontend-critical fields: query rewrite expanded terms, retrieval arrays, rerank top5 citation IDs, answer chunk text, `done.total_tokens`, `done.latency_ms`, and `done.citations` keyed by citation ID. Keep tests resilient to chunk text wording except required citation marker compatibility.</action>
  <verify>cd backend && python3 -m pytest tests/test_agent_workflow.py tests/test_query_api.py</verify>
  <acceptance_criteria>
    - Tests prove every Phase 4 adapter input exists.
    - Tests do not assert fragile natural-language answer wording.
    - Tests pass offline with mock providers.</acceptance_criteria>
  <done>Backend query contract is covered by tests.</done>
</task>

<task type="auto">
  <name>Task 2: Verify provider/env template remains complete</name>
  <files>backend/.env.example, backend/tests/test_provider_config.py</files>
  <read_first>backend/.env.example, backend/app/core/config.py, backend/tests/test_provider_config.py</read_first>
  <action>Ensure `.env.example` still has blank placeholders for base URL, unified API key, embedding model, rerank model, text model, and provider selectors. If helpful, add a small test that required config field names exist in `Settings`; do not create a real `.env`.</action>
  <verify>cd backend && python3 -m pytest tests/test_provider_config.py</verify>
  <acceptance_criteria>
    - Required Bailian/Qwen Plus config remains discoverable.
    - Defaults remain mock/offline.
    - No secret values are introduced.</acceptance_criteria>
  <done>Provider configuration handoff remains safe and complete.</done>
</task>

<task type="auto">
  <name>Task 3: Update integration readiness and validation notes</name>
  <files>.planning/frontend-integration-readiness.md, .planning/phases/03-agent-workflow-and-sse-query-api/03-VALIDATION.md</files>
  <read_first>.planning/frontend-integration-readiness.md, .planning/phases/03-agent-workflow-and-sse-query-api/03-VALIDATION.md, backend/tests/test_query_api.py</read_first>
  <action>Document the finalized Phase 3 SSE event sequence and field mapping for Phase 4. Include a concise manual smoke command/example for `POST /api/query` if practical. Do not edit frontend source in this phase.</action>
  <verify>grep -q "query_rewrite" .planning/frontend-integration-readiness.md && grep -q "POST /api/query" .planning/phases/03-agent-workflow-and-sse-query-api/03-VALIDATION.md</verify>
  <acceptance_criteria>
    - Phase 4 can identify exactly which event fields to map into existing frontend state.
    - Validation notes match implemented/tested backend behavior.
    - No frontend redesign or React code changes are made.</acceptance_criteria>
  <done>Frontend integration handoff is ready for Phase 4.</done>
</task>

<task type="auto">
  <name>Task 4: Run full backend validation</name>
  <files>backend/tests/*</files>
  <read_first>backend/tests/test_query_api.py, backend/tests/test_agent_workflow.py</read_first>
  <action>Run the full backend pytest suite and fix only Phase 3-related failures. Do not repair unrelated issues unless they were introduced by this phase.</action>
  <verify>cd backend && python3 -m pytest</verify>
  <acceptance_criteria>
    - Full backend test suite passes.
    - Any failure triage is documented in the summary if unrelated.
    - Phase 3 can be marked complete after summaries are written.</acceptance_criteria>
  <done>Phase 3 backend validation passes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd backend && python3 -m pytest`
- [ ] Confirm `.planning/frontend-integration-readiness.md` includes final SSE event mapping.
- [ ] Confirm no frontend source files were changed.
- [ ] Confirm no real secrets were added.
</verification>

<success_criteria>
- Phase 3 has automated coverage for agent workflow and SSE endpoint contracts.
- Full backend tests pass offline.
- Frontend integration handoff is updated and actionable.
- Phase remains backend-focused and ready for Phase 4联调.
</success_criteria>

<output>
After completion, create `.planning/phases/03-agent-workflow-and-sse-query-api/03-03-SUMMARY.md` and update Phase 3 completion state through the GSD workflow.
</output>
